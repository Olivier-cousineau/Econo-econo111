// pages/bureau-en-gros.jsx

export default function BureauEnGrosTestPage() {
  return (
    <main style={{ padding: "2rem", maxWidth: 800, margin: "0 auto" }}>
      <h1>Bureau en Gros – Test Route</h1>
      <p>
        If you can see this page, the <code>/bureau-en-gros</code> route is
        correctly registered in the Pages Router.
      </p>
      <p>
        Ensuite, on pourra remplacer ce contenu par la vraie page avec les
        liquidations Bureau en Gros.
      </p>
    </main>
  );
}
