name: 🧩 EconoDeal – Canadian Tire Scraper

on:
  # ⏰ Exécution automatique chaque soir à 21h (heure du Québec)
  schedule:
    - cron: "0 1 * * *"
  # ▶️ Permet de le lancer manuellement depuis l’onglet “Actions”
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Récupère le dépôt GitHub
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Debug : affiche la structure du dépôt (utile si erreur de chemin)
      - name: Show repository structure (debug)
        run: |
          echo "📂 Current repository structure:"
          pwd
          ls -R | head -n 100

      # 3️⃣ Crée le dossier si manquant + ajoute 3 magasins de test
      - name: Ensure required folders and sample IDs
        run: |
          mkdir -p data/canadiantire
          if [ ! -f "data/canadiantire/stores_ids.txt" ]; then
            echo "0271" > data/canadiantire/stores_ids.txt
            echo "0649" >> data/canadiantire/stores_ids.txt
            echo "0418" >> data/canadiantire/stores_ids.txt
            echo "[INFO] Created sample stores_ids.txt ✅"
          fi
          test -f scrape_canadiantire_liquidations.py || (echo "[ERROR] Missing scrape_canadiantire_liquidations.py at repo root ❌" && exit 1)

      # 4️⃣ Configure Python 3.11
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 5️⃣ Installe toutes les dépendances nécessaires
      - name: Install dependencies (Playwright, Requests, etc.)
        run: |
          python -m pip install --upgrade pip
          pip install playwright aiofiles pandas tqdm requests beautifulsoup4 lxml
          python -m playwright install-deps
          python -m playwright install

      # 6️⃣ Lance le scraper principal
      - name: Run EconoDeal – Canadian Tire scraper
        env:
          CT_CONCURRENCY: "3"
          CT_DELAY_BASE: "1.6"
          CT_DELAY_JITTER: "1.4"
        run: |
          python scrape_canadiantire_liquidations.py --stores-file data/canadiantire/stores_ids.txt

      # 7️⃣ Sauvegarde les résultats de scraping comme artefact téléchargeable
      - name: Upload scraping results
        uses: actions/upload-artifact@v4
        with:
          name: canadiantire-results
          path: output_canadiantire/**

      # 8️⃣ Commit automatique des données extraites dans le dépôt
      - name: Commit and push results to repository
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "actions@github.com"
          git add output_canadiantire || true
          git commit -m "🧩 Canadian Tire scrape $(date -u +'%Y-%m-%d %H:%M:%S UTC')" || echo "No new data to commit"
          git push
